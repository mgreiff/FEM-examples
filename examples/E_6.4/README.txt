Complete

Test methods by running testplan4.m and testStressStiff.m (should output
vectors ad matrices with all elements ~1). The problem can then be solved
by running newtC6E4.m, or the result can be viewed directly in result.fig.